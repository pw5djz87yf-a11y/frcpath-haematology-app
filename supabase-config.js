// Public browser configuration only.
// Supabase project URLs and anon keys are designed to be public.
// Never place a service-role key, secret key, password, or private token here.
window.FRCPathSupabaseConfig = Object.freeze({
  url: "",
  anonKey: ""
});
